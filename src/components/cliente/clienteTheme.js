/** Re-export tema global — compatibilidad con imports existentes */
export {
  colors,
  spacing,
  radii,
  shadows,
  typography,
  fontSize,
  MIN_TOUCH_TARGET,
  CONTENT_MAX_WIDTH,
  hitSlop,
} from '../../theme';
